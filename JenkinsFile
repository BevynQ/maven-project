pipeline {
    agent any

    stages {
        stage("Build"){

            steps {
                echo 'building'
                bat 'mvn clean package'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts '**/target/*.war'
                }
            }

        }
        stage ('Deploy to Staging'){
            steps {
                build 'Deploy-to-staging'
            }
        }
    }
}